# 메인페이지 Snap Scroll 설계 요청

## 📋 요약

메인페이지에 처음 진입했을 때 snap scroll이 발동되면서 **Event Banner가 사라지는 과정에서 Navbar와 Hero 섹션 상단 여백이 갑자기 줄어드는 현상**이 발생한다. 스크롤이 멈추는 지점이 Event Banner 높이만큼이 아니라, 그보다 더 위로 올라가서 멈추는 것이 핵심 문제이다.

---

## 🔍 현상 분석

### 증상

1. 메인페이지에 처음 진입하면 snap scroll이 자동으로 발동
2. Event Banner가 위로 스크롤되어 사라짐
3. **문제**: 스크롤이 멈추는 지점이 Event Banner 높이만큼이 아니라, 그 이상으로 올라감
4. 결과적으로 Navbar와 Hero 섹션 사이의 상단 여백이 의도보다 줄어들어 보임

### 원인 분석

현재 `page.tsx`의 레이아웃 구조 (관련 코드: [page.tsx](file:///Users/paul/projects/zelly-web/src/app/page.tsx)):

```tsx
<main className="h-screen flex flex-col bg-white overflow-hidden">
  <div className="flex-1 snap-y snap-mandatory overflow-y-auto">
    <div className="snap-start">          <!-- ① Event Banner wrapper (snap-start) -->
      <EventBanner />
    </div>

    <div className="sticky top-0 z-50">   <!-- ② Navbar (sticky, snap 속성 없음) -->
      <Navbar />
    </div>

    <section className="h-screen snap-start">  <!-- ③ Hero 섹션 (snap-start) -->
      <Hero />
    </section>
    ...
  </div>
</main>
```

#### 핵심 원인: snap point가 Hero 섹션(`③`)에 걸려 있음

- `snap-y snap-mandatory`가 적용된 스크롤 컨테이너 안에서 **snap point**는 `snap-start` 클래스가 있는 요소에 설정됨
- 첫 번째 snap point: `① Event Banner wrapper`의 상단
- 두 번째 snap point: `③ Hero 섹션`의 상단
- Navbar(`②`)는 `sticky top-0`으로 고정 위치이지만 **snap-start가 없음** → snap 계산에 포함되지 않음

스크롤이 snap되면 **③ Hero 섹션의 상단이 스크롤 컨테이너의 최상단(top: 0)으로 끌어올려짐**. 그런데 Navbar는 `sticky top-0`이므로 Hero 섹션 위에 겹쳐서 표시된다. 결과적으로:

- **스크롤되는 양** = Event Banner 높이 + Navbar 높이 (Hero 섹션 상단이 viewport의 최상단으로 가야 하므로)
- **기대하는 스크롤 양** = Event Banner 높이만큼만

이 차이 때문에 Hero 섹션이 Navbar 높이만큼 추가로 올라가서 여백이 줄어든 것처럼 보이는 것이다.

> [!IMPORTANT]
> Navbar(`60px`)가 sticky로 Hero 위에 겹치면서, snap이 Hero 상단을 viewport 최상단까지 끌어올린다. 즉 **Event Banner 높이 + Navbar 높이**만큼 스크롤되어 Navbar 높이(`60px`)만큼 여백이 손실된다.

---

## ✅ 요구사항

### 1. Snap Scroll 멈춤 지점 보정

> **딱 Event Banner만 없어질 높이만큼만 Hero 섹션이 올라가서 멈춰야 한다.**

- Event Banner가 스크롤 아웃된 후, Hero 섹션의 콘텐츠 위치는 Event Banner가 처음부터 없었을 때의 위치와 **동일**해야 함
- Navbar 아래로 Hero 섹션이 자연스럽게 시작되어야 하며, Navbar와의 간격이 변하지 않아야 함

### 2. Navbar 디자인 및 위치

- Navbar는 항상 viewport 최상단에 **sticky**로 고정되어야 함
- Snap scroll 전후로 Navbar의 위치나 크기가 변동되지 않아야 함
- **Navbar 배경은 불투명(opaque)하게 현재 페이지의 배경색이 반영되어야 한다.** 반투명(blur)이나 투명이 아닌, 현재 보이는 섹션의 배경색과 동일한 불투명 배경을 사용하여 **Navbar가 페이지와 하나의 페이지처럼 일체감** 있게 보여야 함
- 이 규칙은 Hero뿐만 아니라 **Problem, Solution, Emotional, Footer 등 모든 섹션**에 동일하게 적용됨. 각 섹션으로 스크롤 이동할 때마다 Navbar 배경색이 해당 섹션의 배경색으로 자연스럽게 전환되어야 함

> [!IMPORTANT]
> Navbar가 아래 섹션의 배경색을 불투명하게 반영하려면, **Navbar가 스크롤 컨테이너 내부에서 sticky로 존재**해야 한다. Navbar를 스크롤 컨테이너 밖에 분리하면 어떤 섹션이 현재 보이는지 감지하기 어렵고, 배경색 전환의 자연스러움이 깨질 수 있다. 따라서 **이 구조를 위반하는 해결 방안은 채택할 수 없다.**

### 3. Event Banner 동작

- Event Banner는 페이지 **최상단**에 위치해야 함 (Navbar 위)
- 페이지 첫 진입 시 Event Banner가 보이고, **첫 번째 snap scroll을 통해 Event Banner가 위로 스크롤되어 사라져야** 함
- Event Banner가 사라진 뒤에는 Navbar가 최상단에 sticky로 고정되어 주요 콘텐츠 탐색에 방해가 없어야 함
- 닫기(X) 버튼을 통해서도 수동으로 Event Banner를 닫을 수 있어야 함

### 4. 초기 로딩 시 시각적 안정성

- 페이지 첫 진입 시 Event Banner → Navbar → Hero 순서로 자연스럽게 표시
- Snap scroll로 Event Banner가 사라지는 과정이 부드럽게(smooth) 진행
- 스크롤 후 Hero 섹션 콘텐츠의 **점프(갑작스런 위치 변화)가 없어야 함**

### 5. 반응형 일관성

- 모바일(sm 이하)과 데스크탑(md 이상) 모두에서 동일하게 동작해야 함
- Event Banner의 높이가 디바이스에 따라 다를 수 있으므로(`74px` / `84px`), 이를 고려한 보정이 필요

---

## 💡 해결 방향 제안

> 아래 방안들은 **요구사항 2(Navbar 불투명 배경색 반영)** 및 **요구사항 3(Event Banner는 snap scroll로 사라짐)**을 모두 만족하는 방안만 남겼다.

### 방안 A: Snap point를 Navbar 상단으로 이동

- Event Banner와 Navbar를 하나의 snap 단위로 묶지 않고, snap이 **Navbar 상단**에서 멈추도록 구조 조정
- Navbar wrapper에 `snap-start`를 부여하고, Hero 섹션이 아닌 Navbar가 snap point가 되도록 변경
- ✅ Navbar는 여전히 스크롤 컨테이너 내부에 sticky로 존재 → 섹션별 배경색 반영 가능
- ✅ Event Banner는 snap 컨테이너 안에서 자연스럽게 스크롤 아웃됨

### 방안 B: `scroll-padding-top` 활용

- 스크롤 컨테이너에 `scroll-padding-top: 60px`(Navbar 높이) 적용
- snap이 Hero 상단을 끌어올릴 때, Navbar 높이만큼의 여유를 두어 실질적으로 **Navbar 바로 아래**에서 멈추게 함
- ✅ 기존 구조를 전혀 변경하지 않음 → Navbar sticky 위치 및 배경색 반영에 영향 없음
- ✅ Event Banner는 기존 snap-start 동작 그대로 유지

> [!TIP]
> **방안 B(`scroll-padding-top`)**가 기존 구조 변경이 가장 적으면서도 모든 요구사항을 충족하는 방법이다. snap 컨테이너에 `scroll-padding-top`을 추가하면, snap 계산 시 Navbar 높이를 자동으로 보정하며, Navbar의 sticky 위치와 배경색 반영 로직에는 전혀 영향을 주지 않는다.

---

## 📐 관련 파일

| 파일                                                                                             | 역할                                    |
| ------------------------------------------------------------------------------------------------ | --------------------------------------- |
| [page.tsx](file:///Users/paul/projects/zelly-web/src/app/page.tsx)                               | 메인 레이아웃, snap scroll 컨테이너     |
| [EventBanner.tsx](file:///Users/paul/projects/zelly-web/src/components/sections/EventBanner.tsx) | 이벤트 배너 (스크롤 아웃 대상)          |
| [Navbar.tsx](file:///Users/paul/projects/zelly-web/src/components/sections/Navbar.tsx)           | 네비게이션 바 (sticky top-0, 높이 60px) |
| [Hero.tsx](file:///Users/paul/projects/zelly-web/src/components/sections/Hero.tsx)               | 히어로 섹션 (snap-start 대상)           |
| [globals.css](file:///Users/paul/projects/zelly-web/src/app/globals.css)                         | 전역 CSS                                |

---

_작성일: 2026-02-15_
